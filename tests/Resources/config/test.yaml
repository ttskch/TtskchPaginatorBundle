framework:
  test: true
  secret: test

twig:
  paths:
    tests/Resources/views: ~

ttskch_paginator:
  page:
    name: page
    range: 2
  limit:
    name: limit
    default: 2
  sort:
    key:
      name: sort
    direction:
      name: direction
      default: asc
  template:
    pager: pager.html.twig
    sortable: sortable.html.twig

services:
  Ttskch\PaginatorBundle\Context.pub:
    alias: Ttskch\PaginatorBundle\Context
    public: true
  twig.pub:
    alias: twig
    public: true
